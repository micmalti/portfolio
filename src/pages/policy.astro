---
import PolicyLayout from "../layouts/PolicyLayout.astro";
import { Content, frontmatter, getHeadings } from "../policy.md";
import type { PolicyFrontmatter } from "../types";

const headings = await getHeadings();
---

<PolicyLayout frontmatter={frontmatter as PolicyFrontmatter} headings={headings}>
  <Content />
</PolicyLayout>